let choices = window.document.querySelectorAll(".choice") ;

choices.forEach((val) => {

    val.addEventListener("click" , () => {

        console.log("User Just Selected His/Her Move") ;

    }) ;

}) ;